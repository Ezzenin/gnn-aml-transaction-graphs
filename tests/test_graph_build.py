"""Тесты граф-билдеров. Заготовка под этап 2.

На этапе 2 здесь появятся тесты построения графа (узел=счёт vs узел=транзакция)
для IBM AML. Сейчас src/graph_build.py — пустая заготовка.
"""
import pytest

pytest.skip("graph_build реализуется на этапе 2", allow_module_level=True)
