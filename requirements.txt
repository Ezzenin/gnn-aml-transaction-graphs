# Версии зафиксированы под окружение (macOS arm64, Python 3.14, CPU/MPS).
# pyg-lib нужен для LinkNeighborLoader (мини-батчи edge-GNN на IBM AML);
# ставится с индексом колёс PyG (строка --find-links ниже). Под GPU/CUDA torch и
# PyG ставить по официальной инструкции PyG.
--find-links https://data.pyg.org/whl/torch-2.10.0+cpu.html
torch==2.10.0
torch_geometric==2.8.0
pyg-lib==0.7.0+pt210
xgboost==3.2.0
scikit-learn==1.9.0
pandas==3.0.1
numpy==2.4.2
networkx==3.6.1
matplotlib==3.10.8
pyyaml==6.0.3
tqdm==4.67.3
pytest==9.0.3
nbconvert==7.17.1
ipykernel==7.2.0
wandb==0.27.2      # логирование экспериментов (вкл. через wandb.enabled или WANDB_MODE)
streamlit==1.58.0  # Streamlit-продукт (Фаза G1: режим антифрод)
plotly==6.8.0      # интерактивная визуализация графа цепочки в продукте

# Опционально:
# lightgbm        # альтернативный градиентный бустинг
# pyvis           # альтернатива plotly для интерактивного графа
